apiVersion: v1
kind: Service
metadata:
  name: vnc-service #The name of the Kubernetes Service to be created in the Kubernetes cluster
spec:
  selector:
    app:  
  type: LoadBalancer #Type of the Kubernetes Service
  ports:
  - protocol: TCP
    name: "5900"
    port: 5900 #Service port
    targetPort: 5900 #The port for the vnc container