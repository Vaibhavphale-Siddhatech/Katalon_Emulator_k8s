apiVersion: v1
kind: Service
metadata:
  name: android-service #The name of the Kubernetes Service to be created in the Kubernetes cluster
spec:
  selector:
    app:  
  type: LoadBalancer #Type of the Kubernetes Service
  ports:
  - protocol: TCP
    name: "4725"
    port: 4725 #Service port
    targetPort: 4725 #The port for the katalon android application container